export class Modul {
    constructor(
        private modulId: Number,
        private bezeichnung: String,
        private fach: String,
        private creditpoints: Number,
        private typ: String
    ) {

    }
}